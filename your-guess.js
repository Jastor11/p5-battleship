
function placePins() {
  //setting up the pin formatting.
  var c = color(232, 26, 26);  // Define color 'c'
  fill(c);  // Use color variable 'c' as fill color
  noStroke();  // Don't draw a stroke around shapes

  //Code your guesses here using the ellipse method! Here's your first one:
  ellipse(100, 65, 20, 20); //This is the first solved pin!

}